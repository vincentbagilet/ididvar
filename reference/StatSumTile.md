# Automatic mean aggregation for tiles

A ggplot2 Stat that sums the values provided in z for each tile

## Usage

``` r
StatSumTile
```

## Format

An object of class `StatSumTile` (inherits from `Stat`, `ggproto`, `gg`)
of length 4.
